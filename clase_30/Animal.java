public class Animal {

    // Caracteristicas (atributos)
    // Como no indicamos ningun modificador se sobreentienden PUBLICOS (public)
    String piel;
    int ojos;
    int edad;
    String habitat = "Mar";

    // Comportamientos (metodos)
    // Como no indicamos ningun modificador se sobreentienden PUBLICOS (public)
    void comer(){
    }

    void respirar(){
    }

    // Este metodo devuelve un valor boolean
    boolean dormir(){
        return true;
    }

    void reproducir(){
    }

    // Este metodo recibe un parametro y devuelve un valor double
    double desplazar(double distancia){
        double x = 0;
        return x;
    }
}
